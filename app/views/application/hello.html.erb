 <% if session[:name] == nil %>
     <div> You dont appear to be logged </div> 
     <%= button_to "Log In", login_path, method: :get %> 
 <% else %> 
    <h1>Hi, <%= session[:name] %>.</h1>
    <%= form_tag ({controller: 'sessions', action: 'destroy', method: 'post'}) do %>
        <input type=submit value='logout'>
    <% end %>
 <% end %> 
